<Window x:Class="MetaQuestTrayTool.Views.ProfileEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Personal profile"
        Height="560"
        Width="500"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource AppBackgroundBrush}"
        Foreground="{DynamicResource AppTextBrush}">
  <DockPanel Margin="20">
    <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Content="Save" Style="{StaticResource AccentButtonStyle}" Click="Save_Click"/>
      <Button Content="Cancel" Style="{StaticResource GhostButtonStyle}" Click="Cancel_Click"/>
    </StackPanel>

    <StackPanel>
      <TextBlock x:Name="ScopeText"
                 Margin="0,0,0,12"
                 TextWrapping="Wrap"
                 Foreground="{DynamicResource AppMutedBrush}"/>

      <TextBlock Text="Name" Foreground="{DynamicResource AppMutedBrush}"/>
      <TextBox x:Name="NameBox" Margin="0,4,0,12" Padding="8,6"/>

      <TextBlock Text="Process name (without .exe)" Foreground="{DynamicResource AppMutedBrush}"/>
      <DockPanel Margin="0,4,0,12">
        <Button DockPanel.Dock="Right"
                Content="From library"
                Margin="8,0,0,0"
                Style="{StaticResource GhostButtonStyle}"
                Click="BrowseLibrary_Click"/>
        <TextBox x:Name="ProcessBox" Padding="8,6"/>
      </DockPanel>

      <TextBlock Text="Platform" Foreground="{DynamicResource AppMutedBrush}"/>
      <ComboBox x:Name="PlatformBox" Margin="0,4,0,12" Padding="6"/>

      <TextBlock Text="Super Sampling" Foreground="{DynamicResource AppMutedBrush}"/>
      <ComboBox x:Name="SuperSamplingBox" Margin="0,4,0,12" Padding="6"/>

      <TextBlock Text="ASW mode" Foreground="{DynamicResource AppMutedBrush}"/>
      <ComboBox x:Name="AswBox" Margin="0,4,0,12" Padding="6"/>

      <TextBlock Text="FOV multiplier (1.00 = unchanged)" Foreground="{DynamicResource AppMutedBrush}"/>
      <TextBox x:Name="FovBox" Margin="0,4,0,12" Padding="8,6" Text="1.00"/>

      <TextBlock Text="CPU priority" Foreground="{DynamicResource AppMutedBrush}"/>
      <ComboBox x:Name="PriorityBox" Margin="0,4,0,12" Padding="6"/>

      <TextBlock Text="Notes" Foreground="{DynamicResource AppMutedBrush}"/>
      <TextBox x:Name="CommentsBox" Margin="0,4,0,0" Padding="8,6"/>
    </StackPanel>
  </DockPanel>
</Window>
