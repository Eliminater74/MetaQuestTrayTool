<UserControl x:Class="MetaQuestTrayTool.Views.Pages.InfoPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <ScrollViewer VerticalScrollBarVisibility="Auto">
    <StackPanel>
      <TextBlock Text="Info" Style="{StaticResource PageTitleStyle}"/>
      <TextBlock Style="{StaticResource PageHintStyle}"
                 Text="Live status for OpenXR, Oculus service, and a connected VR headset. Phones, tablets, and emulators are ignored. ADB commands only run on your trusted VR headset."/>

      <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
        <Button Content="Donate" Style="{StaticResource AccentButtonStyle}" Click="Donate_Click"/>
        <Button Content="Refresh" Style="{StaticResource GhostButtonStyle}" Click="Refresh_Click"/>
        <Button Content="Copy" Style="{StaticResource GhostButtonStyle}" Click="Copy_Click"/>
        <Button Content="Trust this headset" Style="{StaticResource GhostButtonStyle}" Click="Trust_Click"/>
      </StackPanel>

      <TextBlock x:Name="OpenXrBanner" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,6"
                 Foreground="{DynamicResource AppAccentBrush}"/>
      <TextBlock x:Name="ConnectionBanner" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,0,0,6"
                 Foreground="{DynamicResource AppAccentBrush}"/>
      <TextBlock x:Name="SteamTipBanner" TextWrapping="Wrap" Margin="0,0,0,6" Visibility="Collapsed"
                 Foreground="OrangeRed" FontWeight="SemiBold"/>
      <TextBlock x:Name="HeadsetBanner" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,0,0,12"/>

      <Border Background="{DynamicResource AppFieldBrush}"
              BorderBrush="{DynamicResource AppBorderBrush}"
              BorderThickness="1"
              CornerRadius="8"
              Padding="12">
        <TextBox x:Name="ReportBox"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 BorderThickness="0"
                 Background="Transparent"
                 Foreground="{DynamicResource AppTextBrush}"
                 FontFamily="Consolas"
                 FontSize="12"
                 MinHeight="280"/>
      </Border>
    </StackPanel>
  </ScrollViewer>
</UserControl>
